Programacion web 27811: Trabajos, Tareas de todos los parcial 
Abrir desde el segundo parcial:
npm init -y
npm i vite
npm i bootstrap@5.3.8
Proceso en package.json poner:
 "scripts": {
    "dev": "vite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
Para correr:
npm run dev